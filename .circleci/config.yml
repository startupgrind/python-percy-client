version: 2
shared: &shared
    working_directory: ~/circleci-python-percy-client
    steps:
      - checkout  # checkout source code to working directory
      - run:
            command: pipenv install --dev
      - run:
          name: Run tests
          command: py.test
jobs:
  python-27:
      <<: *shared
      docker:
          - image: circleci/python:2.7

workflows:
  version: 2
  build:
    jobs:
      - "python-27"
